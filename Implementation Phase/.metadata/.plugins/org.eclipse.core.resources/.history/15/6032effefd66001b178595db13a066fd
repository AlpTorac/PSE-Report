package gelf.view.diagrams.components;

import static org.junit.jupiter.api.Assertions.*;

import java.awt.Color;

import org.junit.jupiter.api.Test;

class DiagramValueLabelTest implements TestCase {

	@Test
	void test() {
		TestFrame frame = new TestFrame();
		
		PositionInFrame start = new PositionInFrame(400, 400);
		PositionInFrame end = new PositionInFrame(600, 500);
		
		DiagramLabel label = DiagramComponentFactory.getDiagramComponentFactory().createDescriptionLabel(start, end, Color.RED, "Testing \n DiagramLabel", 0, frame.getContentPane());
		
		show(frame);
	}

}
