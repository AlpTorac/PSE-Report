package gelf.controller;


public enum Event {
	
	/*
	 * Load action.
	 */
	LOAD,
	
	/*
	 * Open action.
	 */
	OPEN,
	
	/*
	 * Delete cell action.
	 */
	DELETE,
	
	/*
	 * Remove from project action.
	 */
	REMOVE,
	
	/*
	 * Save liberty file action.
	 */
	SAVE,
	
	/*
	 * Save as a new file action.
	 */
	SAVEAS,
	
	/*
	 * Create a new library from scratch action.
	 */
	CREATE,
	
	/*
	 * Search action.
	 */
	SEARCH,
	
	/*
	 * Edit action.
	 */
	EDIT,
	
	/*
	 * Rename action.
	 */
	RENAME,
	
	/*
	 * Merge libraries action.
	 */
	MERGE,
	
	/*
	 * Copy cell action.
	 */
	COPY,
	
	/*
	 * Move cell action.
	 */
	MOVE,
	
	/*
	 * Paste action
	 */
	PASTE,
	
	/*
	 * Undo action.
	 */
	UNDO,
	
	/*
	 * Redo action.
	 */
	REDO,
	
	/*
	 * Scale action.
	 */
	SCALE,
	
	/*
	 * Compare action.
	 */
	COMPARE,
	
	/*
	 * Interpolate action.
	 */
	INTERPOLATE,
	
	/*
	 * Add filter action.
	 */
	ADDFILTER,
	
	/*
	 * Remove filter action.
	 */
	REMOVEFILTER,
	
	/*
	 * Load project action.
	 */
	LOADPROJECT,
	
	/*
	 * Export a file in CSV format.
	 */
	EXPORT,
	
	/*
	 * Save project action.
	 */
	SAVEPROJECT,
	
	/*
	 * Settings menu action.
	 */
	SETTINGS,
	
	/*
	 * Shortcut settings action.
	 */
	SCSETTINGS,
	
	/*
	 * Preferences settings action.
	 */
	PRSETTINGS,
	
	/*
	 * Language settings action.
	 */
	LASETTINGS;
	
	

}
